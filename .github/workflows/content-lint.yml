name: Content lint

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  content-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run content lint
        run: bash .github/scripts/content-lint.sh
